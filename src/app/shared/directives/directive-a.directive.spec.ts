import { DirectiveADirective } from './directive-a.directive';

describe('DirectiveADirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveADirective();
    expect(directive).toBeTruthy();
  });
});

import { DirectiveBDirective } from './directive-b.directive';

describe('DirectiveBDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveBDirective();
    expect(directive).toBeTruthy();
  });
});
